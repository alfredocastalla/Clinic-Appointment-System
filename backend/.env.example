# Supported database backends: mysql or sqlite
DB_TYPE=sqlite
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=
DB_NAME=database.sqlite
JWT_SECRET=super-secret-jwt-key
ADMIN_EMAIL=admin@clinic.local
ADMIN_PASSWORD=admin123

# If you want to use MySQL locally, set DB_TYPE=mysql
# and provide DB_USERNAME, DB_PASSWORD, DB_HOST, DB_PORT, and DB_NAME
